import { appointmentsFeature } from '@/features/appointments/feature';
import type { FeatureModule } from '@/shared/types/feature';

export const featureModules: FeatureModule[] = [appointmentsFeature];
